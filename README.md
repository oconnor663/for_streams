features

- `.await` in one arm doesn't block progress in other arms
- `continue`/`break`/`return` are supported
- `move` is supported
- `background` is supported
- doesn't require the caller to `.fuse()` things

todo:

- expand this README
